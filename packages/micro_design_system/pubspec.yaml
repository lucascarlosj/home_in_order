name: micro_design_system
ndescription: A new Flutter package project.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  micro_dependencies:
    path: '../micro_dependencies'
  micro_core:
    path: '../micro_core'

flutter:
  uses-material-design: true

